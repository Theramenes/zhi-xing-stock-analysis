# 迭代需求

## 当前版本 (Phase 1-3 已完成)

- 知行B1/趋势/单针下20/缩爆B1 指标
- iFind(freeStockLine 降级) 数据源
- 板块扫描概览 + B1 扫描 + 飞书发布
- 黑名单(688/ST/北交所)过滤

---

## Roadmap

### Phase A: K线本地数据库

**目标**: 全部 A 股的 120 天 K 线存在本地，不再每次拉取

| 步骤 | 内容 | 估时 |
|------|------|------|
| A1 | SQLite 建表 `stock_daily(code, date, open, high, low, close, volume)`, (code,date) 唯一约束 | 30min |
| A2 | 首次全量填充：从 freeStockLine/akshare 拉全市场 K 线（不耗 iFind 配额） | 2h |
| A3 | 每日增量更新：date_sequence 只补每个股票缺的最新一天 | 1h |
| A4 | 交易日统计：根据数据库已有数据+date_sequence 精确补缺 | 30min |
| A5 | B1 计算器适配：从 SQLite 读 candles 而非 stdin/文件 | 30min |

**数据流**：
```
首次: freeStockLine/akshare 批量拉 → SQLite
每日: date_sequence(code, last_date, today) → 补1-2天 → SQLite
B1: SELECT * FROM stock_daily WHERE code=? ORDER BY date DESC LIMIT 120
```

### Phase B: 多数据源降级

**目标**: iFind → freeStockLine → akshare → 本地缓存，自动切换

| 步骤 | 内容 | 估时 |
|------|------|------|
| B1 | 抄 JusticePlutus 的 `DataFetcherManager` 优先级+指数退避模式 | 1h |
| B2 | 接入 akshare/efinance 做免费 K 线源（iFind 配额不够时兜底） | 1h |
| B3 | 安装验证：`pip install akshare efinance`，测试全市场批量拉取 | 30min |
| B4 | 熔断器：单源连续失败 N 次自动降级 | 30min |

**数据源优先级**：
```
① iFind date_sequence  (付费，优先，精确定制)
② iFind cmd_history    (付费，降级)
③ freeStockLine        (免费，不可靠但能用)
④ akshare/efinance     (免费，全市场批量，慢但全)
⑤ 本地缓存 SQLite      (离线，兜底)
```

### Phase C: 关注/持仓管理

**目标**: 用户可维护关注列表 + 持仓状态，扫描自动交叉

| 步骤 | 内容 | 估时 |
|------|------|------|
| C1 | `data/holdings.yaml`：持仓代码+成本价+数量 | 30min |
| C2 | `data/watchlist.yaml`：关注代码+关注原因+添加日期 | 30min |
| C3 | B1 扫描结果自动标记：在关注列表？持仓？B1/近B1 状态 | 30min |
| C4 | 变化追踪：每日对比，J值/评分/信号变化 → 标记"恶化/改善" | 1h |

### Phase D: iFind 日快照（THS_SS）

**目标**: 用日快照替代实时行情，拿当天盘中 OHLCV

| 步骤 | 内容 | 估时 |
|------|------|------|
| D1 | 测试 `THS_SS` 端点：`https://ft.10jqka.com.cn/api/v1/snap_shot` | 30min |
| D2 | 封装到 `ifind_client.py`：`get_daily_snapshot(code)` | 30min |
| D3 | 增量更新流程：每天盘后用日快照补最新一日 K 线 | 30min |

### Phase E: 飞书增强

**目标**: 不只是发文档，加上定时任务、Webhook 推送

| 步骤 | 内容 | 估时 |
|------|------|------|
| E1 | 参考 JusticePlutus 的 `FeishuSender`：Webhook 推送日报摘要 | 1h |
| E2 | 定时任务：cron/GitHub Actions 每日收盘后自动跑扫描→发飞书 | 1h |
| E3 | 配置引导：首次运行时检查 `FEISHU_WEBHOOK_URL` 等，缺失时提示 | 30min |

---

## 实施顺序

```
Phase A (K线数据库)  ← 必须先做，后续所有依赖本地K线
  ↓
Phase B (多数据源)    ← A 把首次填充做好后，B 提供更多数据源选择
  ↓
Phase C (关注/持仓)   ← 有数据库后可以追踪变化
  ↓
Phase D (日快照)      ← 和 A3 互补（增量更新的一种方式）
  ↓
Phase E (飞书增强)    ← 最后的输出层优化
```

## 验证

- **A**: `SELECT COUNT(*) FROM stock_daily` ≥ 5000 × 100
- **B**: 关掉 iFind，自动切到 akshare 跑通一次扫描
- **C**: `python cli.py holdings-review` 输出持仓 B1 变化
- **D**: `get_daily_snapshot("300083")` 返回今日 OHLCV
- **E**: 收到每日飞书推送摘要
